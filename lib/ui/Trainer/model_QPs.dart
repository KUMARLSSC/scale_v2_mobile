import 'package:flutter/material.dart';

class ModelQPS extends StatelessWidget {
  const ModelQPS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
